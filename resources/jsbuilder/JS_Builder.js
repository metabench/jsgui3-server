const {each, Evented_Class} = require('lang-mini');
// Macroscope
class JS_Builder extends Evented_Class {
    constructor(spec) {
        super();
        // Specifying different levels of code for different modules.
        //  Defining platforms that get used by other things.
        //  Go far in terms of compression by using functions, objects and names all within scope.

        // Macroscope

        /*
        Methodology
        -----------

        Build up the bundle / deployment from the lowest level.
        // Tell jsbuilder of JS files and what they do.
        //  What platform level those files are.

        // Do the whole thing based on platforms.
        //  Get everything on the lowest level callable by the levels above.
        //   can use very short names all over the place.

        Properties:


        */

        // .platforms.add...
        // .platforms



    }
}

module.exports = JS_Builder;